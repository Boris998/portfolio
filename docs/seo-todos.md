# SEO Todos

## Sprint 9

- Generate Satori-rendered branded OG images for `/work/[slug]` (currently using hero image directly — see `TODO Sprint 9` comment in `src/routes/work/[slug]/+page.svelte`).
- Include all `/work/[slug]` routes in `sitemap.xml`.
- Include `/writing/[slug]` routes in `sitemap.xml` once Sprint 5 ships.
- Include `/lab` in `sitemap.xml`.
- Add `<link rel="sitemap">` to `<head>` in `+layout.svelte`.
